#include <stdio.h>

int main(void)
{
    int list[] = malloc(3, sizeof(list));
    if (list = NULL)
    {
        return 1;
    }

    for(int i = 0; i < 4; i++)
    {
        list[0] = 1;
        list[1] = 2;
        list[2] = 3;
    }
}
